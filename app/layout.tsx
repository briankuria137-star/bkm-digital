import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BKM DIGITAL | Digital Products for Modern Businesses",
  description:
    "BKM DIGITAL builds digital catalogues, websites, online shops, software and social-media templates for modern businesses.",
  keywords: [
    "BKM DIGITAL",
    "digital products Kenya",
    "business websites Kenya",
    "online catalogues",
    "online shops",
    "business software",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
